# 📊 Анализ производительности для сервера 2 CPU 2GB RAM 60GB SSD

## 🎯 **DAU 200-300 пользователей - Анализ ресурсов**

---

## 💻 **Текущая архитектура приложения:**

### 🏗️ **Технологический стек:**
- **Go 1.25.8** - компилируемый язык, низкое потребление памяти
- **Gin framework** - легковесный HTTP фреймворк
- **PostgreSQL** - реляционная БД
- **Redis** - кэширование остатков
- **JWT** - stateless аутентификация

### 📦 **Основные компоненты:**
- **Web сервер** (Gin)
- **API обработчики** (REST)
- **База данных** (PostgreSQL)
- **Кэш** (Redis)
- **Вебхуки** (МойСклад)

---

## 📊 **Расчет нагрузки для DAU 200-300:**

### 🔄 **Пользовательская активность:**
```
DAU: 250 пользователей (среднее)
Сессий на пользователя: 3-5 в день
Длительность сессии: 5-15 минут
Запросов на сессию: 20-50
```

### 📈 **Общая нагрузка:**
```
Запросов в день: 250 × 4 × 35 = 35,000 запросов
Запросов в час: 35,000 ÷ 24 = ~1,460 RPH
Запросов в минуту: ~24 RPM
Пиковая нагрузка: ~50-100 RPM
```

---

## 🖥️ **Анализ ресурсов сервера:**

### 💾 **Потребление RAM:**

#### **Базовое потребление:**
- **Go приложение**: ~50-100MB
- **PostgreSQL**: ~200-400MB
- **Redis**: ~50-100MB
- **ОС и системные службы**: ~200-300MB

#### **Потребление под нагрузкой:**
- **Go приложение**: ~100-200MB
- **PostgreSQL**: ~400-600MB
- **Redis**: ~100-150MB
- **ОС и кэш**: ~300-400MB

**Итого: ~900-1450MB (из 2GB доступных)**

### ⚡ **Потребление CPU:**

#### **Базовая нагрузка:**
- **Go приложение**: 5-10% CPU
- **PostgreSQL**: 2-5% CPU
- **Redis**: 1-3% CPU

#### **Под нагрузкой (DAU 250):**
- **Go приложение**: 15-30% CPU
- **PostgreSQL**: 10-20% CPU
- **Redis**: 5-10% CPU

**Итого: ~30-60% CPU (из 200% доступных)**

---

## 💾 **Анализ дискового пространства:**

### 📊 **Требования к SSD:**

#### **Приложение и ОС:**
- **ОС (Ubuntu/CentOS)**: ~10-20GB
- **Go приложение**: ~100MB
- **Логи**: ~1-5GB/месяц
- **Временные файлы**: ~1-2GB

#### **База данных:**
- **PostgreSQL данные**: ~5-15GB/год
- **Redis данные**: ~100-500MB
- **Бэкапы**: ~10-30GB/год

#### **Статические файлы:**
- **Изображения товаров**: ~1-5GB
- **CSS/JS**: ~50MB
- **Шаблоны**: ~10MB

**Итого: ~20-40GB (из 60GB доступных)**

---

## ✅ **Вердикт по ресурсам:**

### 🎯 **CPU: 2 CPU - ДОСТАТОЧНО ✅**
- **Пиковая нагрузка**: ~60% (1.2 из 2 ядер)
- **Средняя нагрузка**: ~30% (0.6 из 2 ядер)
- **Запас мощности**: 40% для пиков

### 💾 **RAM: 2GB - ДОСТАТОЧНО ✅**
- **Максимальная нагрузка**: ~1.45GB
- **Свободно**: ~550MB
- **Запас**: 27% для неожиданных нагрузок

### 💾 **SSD: 60GB - ДОСТАТОЧНО ✅**
- **Требования**: ~40GB
- **Свободно**: ~20GB
- **Запас**: 33% для роста

---

## 📈 **Производительность под нагрузкой:**

### ⚡ **Время ответа:**
- **Легкие запросы** (статика): < 50ms
- **API запросы** (с кэшем): 50-150ms
- **Базовые запросы** (без кэша): 100-300ms
- **Сложные запросы** (база): 200-500ms

### 🔄 **Пропускная способность:**
- **RPS (requests per second)**: 20-50
- **Concurrent users**: 50-100 одновременно
- **Database connections**: 20-50

---

## 🚀 **Оптимизации для максимальной эффективности:**

### ⚙️ **Конфигурация PostgreSQL:**
```bash
# postgresql.conf оптимизация для 2GB RAM
shared_buffers = 256MB          # 12.5% от RAM
effective_cache_size = 1.5GB     # 75% от RAM
work_mem = 4MB                   # Для сортировок
maintenance_work_mem = 64MB      # Для индексов
max_connections = 100             # Максимальные подключения
checkpoint_completion_target = 0.9
wal_buffers = 16MB
```

### ⚙️ **Конфигурация Redis:**
```bash
# redis.conf оптимизация
maxmemory 512MB
maxmemory-policy allkeys-lru
save 900 1
save 300 10
save 60 10000
```

### ⚙️ **Конфигурация Go приложения:**
```bash
# .env файл
GOMAXPROCS=2                    # Использовать все CPU
GOGC=100                        # GC агрессивность
GOMEMLIMIT=256MiB               # Ограничение памяти
```

---

## 📊 **Мониторинг и метрики:**

### 📈 **Ключевые метрики для мониторинга:**
```bash
# CPU
top -p $(pgrep -f "hozdacha")
htop

# RAM
free -h
ps aux --sort=-%mem | head

# Диск
df -h
du -sh /var/log/postgresql/

# Сеть
netstat -an | grep :8080
ss -tuln | grep :8080
```

### 🚨 **Пороги для оповещений:**
```bash
CPU > 80% (продолжительное время)
RAM > 1.6GB (80% от 2GB)
Диск > 48GB (80% от 60GB)
RPS > 100
Response time > 1s
```

---

## 🔧 **Дополнительные оптимизации:**

### 📦 **PostgreSQL:**
- **Индексы** для частых запросов
- **Partitioning** для больших таблиц
- **Vacuum** настройка
- **Connection pooling**

### 🗄️ **Redis:**
- **Pipeline** для множественных запросов
- **Cluster** если нужно масштабирование
- **Persistence** настройка

### 🌐 **Go приложение:**
- **Connection pooling** для БД
- **Response caching**
- **Compression** для API
- **Rate limiting**

---

## 📊 **Тестирование нагрузки:**

### 🧪 **Рекомендуемые инструменты:**
```bash
# Apache Bench
ab -n 1000 -c 10 http://localhost:8080/

# Wrk (более реалистичный)
wrk -t12 -c400 -d30s http://localhost:8080/

# Hey (простой)
hey -n 1000 -c 50 http://localhost:8080/api/products
```

### 📈 **Ожидаемые результаты:**
- **Successful requests**: >99%
- **Failed requests**: <1%
- **Average response time**: <200ms
- **95th percentile**: <500ms

---

## 🎯 **Рекомендации по масштабированию:**

### 📈 **Когда нужно обновлять:**
- **DAU > 500** -考虑 4GB RAM
- **DAU > 1000** -考虑 4GB RAM + 4 CPU
- **DAU > 2000** -考虑 8GB RAM + 8 CPU

### 🔄 **Горизонтальное масштабирование:**
- **Load balancer** (Nginx)
- **Multiple app instances**
- **Database read replicas**
- **Redis cluster**

---

## 🎉 **Итоговый вердикт:**

### ✅ **Сервер 2 CPU 2GB RAM 60GB SSD - ИДЕАЛЬНО ПОДХОДИТ!**

#### 🎯 **Для DAU 200-300:**
- **CPU**: 60% utilization ✅
- **RAM**: 72% utilization ✅  
- **SSD**: 67% utilization ✅
- **Производительность**: Отличная ✅
- **Масштабируемость**: Есть запас ✅

#### 💡 **Преимущества:**
- **Экономичность**: Низкая стоимость
- **Производительность**: Быстрый отклик
- **Надежность**: Запас ресурсов
- **Масштабируемость**: Простое обновление

#### 🚀 **Рекомендации:**
1. **Настроить мониторинг** ресурсов
2. **Оптимизировать PostgreSQL** конфигурацию
3. **Включить кэширование** везде где возможно
4. **Настроить бэкапы** базы данных
5. **Мониторить логи** ошибок

---

**🎉 Ваш сервер отлично подойдет для DAU 200-300 с хорошим запасом производительности!**
